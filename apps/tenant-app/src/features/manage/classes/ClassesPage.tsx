/** ClassesPage — placeholder. Full implementation in later FE phase. */
export default function ClassesPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">ClassesPage</h1>
      <p className="text-sm text-muted-foreground mt-1">
        Coming in next phase.
      </p>
    </div>
  );
}
